<Bindings>
	<Binding name="SHOWCONFIG" header="REPUTATIONGUIDE" runOnUp="true">
		if ( keystate == "down" ) then
			REP_OpenSettings()
		end
	</Binding>
	<Binding name="SHOWDETAILS" runOnUp="true">
		if ( keystate == "down" ) then
			REP_ToggleDetailWindow()
		end
	</Binding>
</Bindings>
